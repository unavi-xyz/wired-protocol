package wired:math;

world lib {
  import types;
}

interface types {
  record vec2 {
    x: f32,
    y: f32,
  }

  record vec3 {
    x: f32,
    y: f32,
    z: f32,
  }

  record quat {
    x: f32,
    y: f32,
    z: f32,
    w: f32,
  }
}
