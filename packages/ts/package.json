{
  "name": "@wired-protocol/types",
  "version": "0.1.0",
  "license": "MIT",
  "files": [
    "dist"
  ],
  "scripts": {
    "generate": "pnpm clean && mkdir dist && protoc --ts_out dist --proto_path ../../protos ../../protos/*.proto ../../protos/**/**/*.proto",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@protobuf-ts/plugin": "^2.9.0"
  }
}
