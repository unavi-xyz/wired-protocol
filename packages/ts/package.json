{
  "name": "@wired-protocol/types",
  "version": "0.7.0",
  "description": "Typescript definitions for The Wired Protocol",
  "repository": {
    "type": "git",
    "url": "https://github.com/wired-protocol/spec/packages/ts.git"
  },
  "files": [
    "dist"
  ],
  "license": "MIT",
  "type": "module",
  "scripts": {
    "generate": "pnpm clean && mkdir dist && protoc --ts_out dist --proto_path ../../protos ../../protos/*.proto ../../protos/**/**/*.proto",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@protobuf-ts/plugin": "^2.9.0"
  }
}
