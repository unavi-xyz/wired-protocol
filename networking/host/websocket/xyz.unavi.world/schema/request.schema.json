{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "../../schema/message.schema.json",
  "title": "Request WebSocket Message Schema",
  "description": "A schema for WebSocket messages sent from the client to server.",
  "oneOf": [
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.join" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.join" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.leave" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.leave" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.chat.send" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.chat.send" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.user.avatar" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.user.avatar" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.user.handle" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.user.handle" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.user.name" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.user.name" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.world.user.falling" },
        "data": { "$ref": "#/definitions/xyz.unavi.world.user.falling" }
      }
    }
  ],
  "definitions": {
    "xyz.unavi.world.join": {
      "type": "string",
      "description": "The URI of the world to join"
    },
    "xyz.unavi.world.leave": {
      "type": "string",
      "description": "The URI of the world to leave"
    },
    "xyz.unavi.world.chat.send": {
      "type": "string",
      "description": "The message to send"
    },
    "xyz.unavi.world.user.avatar": {
      "type": ["string", "null"],
      "description": "The URI of the user's avatar"
    },
    "xyz.unavi.world.user.handle": {
      "type": ["string", "null"],
      "description": "The user's handle"
    },
    "xyz.unavi.world.user.name": {
      "type": ["string", "null"],
      "description": "The user's name"
    },
    "xyz.unavi.world.user.falling": {
      "type": "boolean",
      "description": "Whether the user is falling"
    }
  }
}
