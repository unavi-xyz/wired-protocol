{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$ref": "../../schema/message.schema.json",
  "title": "Request WebSocket Message Schema",
  "description": "A schema for WebSocket messages sent from the client to server.",
  "oneOf": [
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.audio.pause" },
        "data": { "type": "boolean" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.transport.connect" },
        "data": { "$ref": "#/definitions/xyz.unavi.webrtc.transport.connect" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.transport.create" },
        "data": { "$ref": "#/definitions/xyz.unavi.webrtc.transport.create" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.router.rtpCapabilities.get" },
        "data": { "type": "null" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.rtpCapabilities.set" },
        "data": { "$ref": "./webrtc.rtpCapabilities.schema.json" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.produce" },
        "data": { "$ref": "./webrtc.rtpParameters.schema.json" }
      }
    },
    {
      "properties": {
        "id": { "const": "xyz.unavi.webrtc.produceData" },
        "data": { "$ref": "./webrtc.sctpStreamParameters.schema.json" }
      }
    }
  ],
  "definitions": {
    "xyz.unavi.webrtc.transport.connect": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "The type of transport to connect",
          "enum": ["producer", "consumer"]
        },
        "dtlsParameters": { "$ref": "./webrtc.dtlsParameters.schema.json" }
      },
      "required": ["type", "dtlsParameters"]
    },
    "xyz.unavi.webrtc.transport.create": {
      "type": "string",
      "description": "The type of transport to create",
      "enum": ["producer", "consumer"]
    }
  }
}
